# AWS EC2 Python

Deploys:
- VPC and subnets
- User-specified number of EC instances distributed across subnets

## Demonstrated Capabilities
- Local component resource (`Network`)
- Remote Pulumi component(s) (`StackSettings`)
- Using off-the-shelf Python package (`netaddr`)
- For-loops