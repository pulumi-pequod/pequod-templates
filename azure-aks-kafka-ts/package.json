{
    "name": "azure-ts-aks-helm",
    "devDependencies": {
        "@types/node": "^10.0.0"
    },
    "scripts": {
        "install": "npm run esc:cologin && npm run copkgs:install",
        "esc:cologin": "pulumi env run pequod/cloud-access/aws-access -- npm run co:login",
        "co:login": "aws codeartifact login --tool npm --region us-east-2 --repository pequod-codeartifact-repo --domain pequod-codeartifact-domain",
        "copkgs:install": "npm i @pequod/k8sdatadog @pequod/kafkacluster --no-save"
    },
    "dependencies": {
        "@pulumi-pequod/stackmgmt": "file:sdks/pulumi-pequod-stackmgmt",
        "@pulumi/azure": "^6.22.0",
        "@pulumi/azure-native": "^3.0.0",
        "@pulumi/azuread": "^6.0.0",
        "@pulumi/confluentcloud": "^2.0.0",
        "@pulumi/kubernetes": "^4.0.0",
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/random": "^4.0.0",
        "@pulumi/tls": "^5.0.0"
    }
}
