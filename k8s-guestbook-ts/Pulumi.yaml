name: ${PROJECT}
runtime: nodejs
packages:
  stackmgmt: https://github.com/pulumi-pequod/component-stackmgmt@v2.2.1
  k8sapp: https://github.com/pulumi-pequod/component-k8sapp@v1.0.3
description: ${DESCRIPTION}
template:
  description: K8s guestbook application deployed to shared cluster.
  config:
    baseInfraStackName: 
      description: Name of base k8s infra stack to use in form of "project/stack"
      default: shared-dev-eks/dev 
