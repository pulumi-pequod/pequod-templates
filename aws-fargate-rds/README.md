# AWS ECS Fargate App with RDS Backend

Deploys:
- Network: VPC, Subnets, Security Groups
- DB Backend: MySQL RDS
- Frontend: Fargate ECS app

## Demonstrated Capabilities
- Pulumi AWSx package
- Local component resources (`Network`, `DB`, `Frontend`)
- Remote Pulumi component(s) (`StackSettings`)